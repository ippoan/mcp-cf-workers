# wake test

Session B が作成した cross-session wake テスト用の PR。
次のセッション (session C) がこの PR にコメントすることで、
購読中の session B が `<github-webhook-activity>` で wake するかを確認する。

Refs: cross-session wake test

## round 2 (draft)

前回 #20 は即 merge されてしまったため、今回は draft PR として open のまま維持し、
merged 状態でない PR で wake が配送されるかを確認する。
