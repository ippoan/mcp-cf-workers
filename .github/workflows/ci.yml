name: ci
on:
  pull_request: {}
  push:
    branches: [main]
jobs:
  ci:
    # NOTE: pinned to the in-flight branch until ci-workflows#PR merges.
    # Flip back to `@main` after merge.
    uses: ippoan/ci-workflows/.github/workflows/lib-ci.yml@claude/intelligent-pascal-11ZHO
    with:
      node_version: '20'
